# Lila Chakra Admin Panel

Административная панель для проекта "Lila Chakra" (Цифровая Лила). Разработана для управления пользователями, играми и контентом приложения.

## 🛠 Технологический стек

- **Framework:** [Quasar v2](https://quasar.dev/) (Vue 3 Composition API)
- **Language:** TypeScript
- **State Management:** [Pinia](https://pinia.vuejs.org/)
- **Testing:** [Vitest](https://vitest.dev/) + Vue Test Utils
- **Charts:** [ApexCharts](https://apexcharts.com/) (via vue3-apexcharts)
- **Build Tool:** Vite
- **Linter/Formatter:** ESLint + Prettier

## 🚀 Начало работы

### Требования

- Node.js v20+
- Yarn

### Установка зависимостей

```bash
yarn install
```

### Запуск в режиме разработки

```bash
yarn dev
```
Приложение по умолчанию запускается на порту `9000`.
Адрес: `http://localhost:9000`

## 🧪 Тестирование

Проект разрабатывается с применением методологии TDD.

```bash
# Запуск Unit тестов
yarn test:unit

# Запуск тестов с отслеживанием изменений
yarn test
```

## 📦 Сборка и Линтинг

```bash
# Проверка типов и линтинг
yarn lint

# Сборка для продакшена (SPA mode)
yarn build
```

## 📂 Структура проекта

```
src/
├── boot/          # Инициализация (axios и др.)
├── components/    # Переиспользуемые UI компоненты
├── composables/   # Vue Composables (useTheme и др.)
├── css/           # Глобальные стили и переменные SCSS
├── layouts/       # Макеты страниц (MainLayout)
├── pages/         # Страницы (Login, Dashboard, Users)
├── router/        # Конфигурация маршрутизации
├── stores/        # Pinia хранилища состояния (auth, users)
└── types/         # TypeScript интерфейсы
tests/
└── unit/          # Unit тесты (Vitest)
```

## 🔐 Основной функционал

1.  **Аутентификация**: Вход по логину/паролю, JWT токены, защита маршрутов.
2.  **Dashboard**: Обзорная статистика (KPI карточки, график регистраций).
3.  **Пользователи**: Просмотр списка пользователей с пагинацией (Server-side pagination).
4.  **Темы**: Поддержка темной и светлой темы (синхронизация с системой).

## 🎨 Дизайн

Дизайн основан на токенах из основного приложения (`miniapp`), адаптированных под десктопный интерфейс администратора.
